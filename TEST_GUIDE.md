# 订阅消息功能测试指南

## 一、准备工作

### 1. 检查配置

**服务端配置检查：**

```bash
cd server
# 检查是否已安装依赖
pip list | grep -E "flask|requests|apscheduler"

# 如果没有安装，执行
pip install -r requirements.txt
```

**环境变量配置：**

创建 `server/.env` 文件（如果还没有）：

```env
WX_APPID=你的小程序AppID
WX_APPSECRET=你的小程序AppSecret
WX_TEMPLATE_ID=_qZfC75otflYg8nc1suRZK27Ke-mzc_sh3Vtpv8tr2w
```

**小程序配置检查：**

- 确认 `pages/add/add.js` 中的模板ID已配置
- 确认 `utils/api.js` 中的服务端地址配置正确

### 2. 微信公众平台配置

1. 登录 [微信公众平台](https://mp.weixin.qq.com/)
2. 进入 **开发** -> **开发管理** -> **开发设置**
3. 确认 **服务器域名** 已配置（开发环境可跳过）
4. 进入 **功能** -> **订阅消息**，确认模板已申请

## 二、服务端测试

### 1. 启动服务端

```bash
cd server
python app.py
```

**预期输出：**
```
启动 Flask 服务...
APPID: your-appid
模板ID: _qZfC75otflYg8nc1suRZK27Ke-mzc_sh3Vtpv8tr2w
 * Running on http://0.0.0.0:5000
```

### 2. 测试健康检查接口

打开浏览器或使用 curl：

```bash
curl http://localhost:5000/api/health
```

**预期响应：**
```json
{
    "errcode": 0,
    "errmsg": "success",
    "status": "healthy"
}
```

### 3. 测试获取 access_token

在服务端日志中查看，启动时会自动获取 access_token。

**检查日志：**
```
获取 access_token 成功
```

### 4. 测试用户登录接口（获取 openid）

```bash
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"code": "test_code"}'
```

**注意**：这里需要使用真实的 code，可以从小程序端获取。

### 5. 测试创建提醒接口

```bash
curl -X POST http://localhost:5000/api/reminder \
  -H "Content-Type: application/json" \
  -d '{
    "openid": "test_openid",
    "title": "测试提醒",
    "time": "明天 10:00",
    "reminderTime": 1704067200000,
    "enableSubscribe": true
  }'
```

**预期响应：**
```json
{
    "errcode": 0,
    "errmsg": "success",
    "data": {
        "id": 1704067200000
    }
}
```

**检查日志：**
```
创建提醒成功: 1704067200000
已安排提醒任务: 1704067200000, 时间: 2024-01-01 10:00:00
```

## 三、小程序端测试

### 1. 配置服务端地址

编辑 `utils/api.js`：

```javascript
// 开发环境
const API_BASE_URL = 'http://localhost:5000/api'

// 或者使用局域网IP（真机测试时）
// const API_BASE_URL = 'http://192.168.1.100:5000/api'
```

**注意**：真机测试时，需要使用电脑的局域网IP地址，不能使用 `localhost`。

### 2. 在微信开发者工具中测试

#### 步骤1：打开项目
1. 打开微信开发者工具
2. 导入项目目录
3. 确认 AppID 已配置

#### 步骤2：测试添加提醒
1. 点击右下角"+"按钮
2. 输入提醒内容
3. 选择提醒时间
4. 开启"开启消息提醒"开关
5. 点击"保存"

**预期行为：**
- 弹出订阅消息授权弹窗
- 用户点击"允许"后，显示"订阅成功"
- 显示"添加成功"提示
- 返回首页，新提醒显示在列表顶部

#### 步骤3：检查控制台
打开开发者工具的"调试器" -> "Console"，查看日志：

```
提醒已保存到服务端，将在指定时间发送订阅消息
```

### 3. 真机测试

#### 配置步骤：

1. **确保手机和电脑在同一局域网**

2. **获取电脑IP地址：**
   ```bash
   # macOS/Linux
   ifconfig | grep "inet "
   
   # Windows
   ipconfig
   ```

3. **修改小程序代码中的服务端地址：**
   ```javascript
   // utils/api.js
   const API_BASE_URL = 'http://192.168.1.100:5000/api'  // 替换为你的IP
   ```

4. **配置服务端允许跨域：**
   服务端已配置 CORS，无需额外配置

5. **在微信开发者工具中：**
   - 点击"预览"
   - 用微信扫码
   - 在手机上测试

## 四、端到端测试流程

### 完整测试步骤：

1. **启动服务端**
   ```bash
   cd server
   python app.py
   ```

2. **在小程序中添加提醒**
   - 打开小程序
   - 点击"+"按钮
   - 输入："测试提醒 - 明天10点开会"
   - 选择时间：明天 10:00
   - 开启订阅开关
   - 点击"保存"
   - 授权订阅消息

3. **验证服务端接收**
   查看服务端日志：
   ```
   创建提醒成功: 1704067200000
   已安排提醒任务: 1704067200000, 时间: 2024-01-02 10:00:00
   ```

4. **测试定时发送（模拟）**
   
   为了快速测试，可以修改提醒时间为当前时间后几分钟：
   
   ```python
   # 在 server/app.py 的 schedule_reminder 函数中
   # 临时修改为当前时间后1分钟（仅用于测试）
   reminder_time = datetime.now() + timedelta(minutes=1)
   ```

5. **等待提醒时间到达**
   - 等待到指定时间
   - 查看服务端日志：
     ```
     提醒发送成功: 1704067200000
     ```
   - 检查微信是否收到订阅消息

## 五、常见问题排查

### 问题1：服务端无法启动

**错误：** `ModuleNotFoundError: No module named 'flask'`

**解决：**
```bash
pip install -r requirements.txt
```

### 问题2：获取 access_token 失败

**检查：**
1. AppID 和 AppSecret 是否正确
2. 网络连接是否正常
3. 查看服务端日志错误信息

**解决：**
```python
# 检查 .env 文件中的配置
WX_APPID=正确的AppID
WX_APPSECRET=正确的AppSecret
```

### 问题3：小程序无法连接服务端

**错误：** `request:fail`

**检查：**
1. 服务端是否已启动
2. 服务端地址是否正确
3. 真机测试时是否使用了局域网IP

**解决：**
- 开发环境：使用 `http://localhost:5000`
- 真机测试：使用 `http://192.168.x.x:5000`
- 检查防火墙设置

### 问题4：订阅消息授权失败

**错误：** 用户点击"拒绝"或授权失败

**检查：**
1. 模板ID是否正确
2. 模板是否已申请并通过
3. 用户是否已授权过（需要重新授权）

**解决：**
- 确认模板ID：`_qZfC75otflYg8nc1suRZK27Ke-mzc_sh3Vtpv8tr2w`
- 检查微信公众平台中的模板状态

### 问题5：订阅消息未发送

**检查：**
1. 服务端定时任务是否正常
2. 提醒时间是否已过
3. 服务端日志是否有错误

**解决：**
```python
# 查看定时任务列表
scheduler.print_jobs()

# 检查提醒时间
reminder_time = datetime.fromtimestamp(reminder['reminderTime'] / 1000)
print(f"提醒时间: {reminder_time}, 当前时间: {datetime.now()}")
```

### 问题6：openid 获取失败

**检查：**
1. code 是否有效（code 只能使用一次）
2. AppID 和 AppSecret 是否正确

**解决：**
- 确保每次调用都使用新的 code
- 检查微信公众平台的 AppID 和 AppSecret

## 六、调试技巧

### 1. 查看服务端日志

服务端会输出详细日志：
```
INFO:__main__:获取 access_token 成功
INFO:__main__:创建提醒成功: 1704067200000
INFO:__main__:已安排提醒任务: 1704067200000, 时间: 2024-01-02 10:00:00
```

### 2. 查看小程序日志

在微信开发者工具中：
- 打开"调试器" -> "Console"
- 查看网络请求："Network"
- 查看存储数据："Storage"

### 3. 测试订阅消息发送

可以手动调用发送接口测试：

```python
# 在 Python 控制台中
from app import send_subscribe_message

result = send_subscribe_message(
    openid='用户的openid',
    template_id='_qZfC75otflYg8nc1suRZK27Ke-mzc_sh3Vtpv8tr2w',
    page='pages/index/index',
    data={
        'thing1': {'value': '测试提醒'},
        'time2': {'value': '明天 10:00'}
    }
)
print(result)
```

### 4. 检查定时任务

```python
# 在 Python 控制台中
from app import scheduler

# 查看所有任务
print(scheduler.get_jobs())

# 查看特定任务
job = scheduler.get_job('reminder_1704067200000')
print(job)
```

## 七、测试检查清单

- [ ] 服务端成功启动
- [ ] 健康检查接口正常
- [ ] access_token 获取成功
- [ ] 小程序可以连接服务端
- [ ] 用户登录接口正常（获取 openid）
- [ ] 创建提醒接口正常
- [ ] 订阅消息授权成功
- [ ] 定时任务已创建
- [ ] 订阅消息成功发送
- [ ] 微信收到订阅消息

## 八、快速测试脚本

创建一个测试脚本 `server/test.py`：

```python
import requests
import json
from datetime import datetime, timedelta

BASE_URL = 'http://localhost:5000/api'

# 1. 健康检查
print("1. 测试健康检查...")
r = requests.get(f'{BASE_URL}/health')
print(json.dumps(r.json(), indent=2, ensure_ascii=False))

# 2. 创建提醒（需要真实的 openid）
print("\n2. 测试创建提醒...")
reminder_time = int((datetime.now() + timedelta(minutes=5)).timestamp() * 1000)
data = {
    'openid': 'test_openid_123',
    'title': '测试提醒',
    'time': '5分钟后',
    'reminderTime': reminder_time,
    'enableSubscribe': True
}
r = requests.post(f'{BASE_URL}/reminder', json=data)
print(json.dumps(r.json(), indent=2, ensure_ascii=False))

# 3. 获取提醒列表
print("\n3. 测试获取提醒列表...")
r = requests.get(f'{BASE_URL}/reminders', params={'openid': 'test_openid_123'})
print(json.dumps(r.json(), indent=2, ensure_ascii=False))
```

运行测试：
```bash
cd server
python test.py
```

## 九、生产环境测试

生产环境测试前，需要：

1. **配置 HTTPS**
   - 微信小程序要求使用 HTTPS
   - 配置 SSL 证书

2. **配置域名**
   - 在微信公众平台配置服务器域名
   - 确保域名已备案

3. **使用数据库**
   - 替换内存存储为数据库
   - 确保数据持久化

4. **监控和日志**
   - 配置日志系统
   - 设置监控告警

## 十、联系支持

如果遇到问题，请检查：
1. 服务端日志
2. 小程序控制台日志
3. 微信公众平台配置
4. 网络连接状态

祝测试顺利！🎉

